# fair-map

TODO:
* The interface should have an "Active Layer" that is highlighted. We can start by highlighting the
  first layer in the list.. but clicking a layer in the panel should make it active. All new markers and
  image uploads should go to this layer